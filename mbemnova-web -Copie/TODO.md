# TODO

- [ ] Update create annonce form to treat `quartier` as a free text field (no quartier-id selection entity).
- [ ] Ensure publish payload sends `quartier: string` and correct `localisationId` (if required by backend).
- [ ] Remove UI dependency on `localisationId` being set from quartier selection.
- [ ] Update any mocks/interceptors that assume quartier comes from localisation entity.
- [ ] Build/test compilation.

